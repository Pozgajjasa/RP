\documentclass[11pt]{article}
\usepackage[a4paper, margin=2.5cm]{geometry}

% Jezik in kodiranje
\usepackage[slovene]{babel}
\usepackage[utf8]{inputenc}

% Naslov in avtor
\title{Brownovo gibanje}
\author{Matej Rojec}
\date{}

% Matematični simboli in okolja
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}

% Slog za definicije
\theoremstyle{definition}
\newtheorem{definicija}{Definicija}

% Slog za izreke
\theoremstyle{plain}
\newtheorem{izrek}{Izrek}

% slika
\usepackage{graphicx}

% Pisani F
\newcommand{\f}{\mathcal{F}}

\begin{document}

\maketitle

Brownovo gibanje (več v \cite{karaktzas1991}) je intuitivno slučajen proces,
ki predstavlja naključno gibanje delcev v mediju.
    
    % Slika: PerrinPlot2.pdf
    \begin{figure}[h!]
        \centering
        \includegraphics[width=0.4\textwidth]{PerrinPlot2.pdf}
        % Napis pod sliko: 
        \caption{Reprodukcija slike iz Jean Baptiste Perrin, \emph{Mouvement brownien et réalité moléculaire}, Ann. de Chimie et de Physique (VIII) 18, 5--114, 1909}
    \end{figure}

    % Začetek definicije
    \begin{definicija}
    Standardno Brownovo gibanje $\{B_t\}_{t \geq 0}$ je slučajen proces z naslednjimi lastnostmi: 
    \begin{enumerate}
        \item $B_0 = 0$.
        \item Prirastki $B_{t_n} - B_{t_{n-1}}, B_{t_{n-1}} - B_{t_{n-2}}, \ldots, B_2 - B_1, B_1 - B_0$ so neodvisne slučajne spremenljivke, za vsak $t_0 \leq t_1 \leq \cdots \leq t_{n-1} \leq t_n$.
        \item Za vsak $t \geq 0$ in $h > 0$ velja $B_{t+h} - B_t \sim \mathcal{N}(0, h)$.
        \item Funkcija $t \mapsto B_t$ je zvezna skoraj gotovo.
    \end{enumerate}
    \end{definicija}
    % Konec definicije
    
    Preden zapišemo izrek, definirajmo še pojem časa ustavljanja.
    
    % Začetek definicije
    \begin{definicija}
        Slučajna spremenljivka $\tau$ na verjetnostnem prostoru $(\Omega, \f, P)$ z vrednostmi v $\mathbb{R}^+$
        je čas ustavljanja glede na filtracijo $(\f_t)_{t\in T}$, če velja: $\forall t \in T : \{\tau \leq t\} \in \f_t$.
    \end{definicija}

    % Konec definicije

    Zdaj lahko zapišemo izrek \ref{thm:stopped_brownian}.

    % Začetek izreka
    \begin{izrek}\label{thm:stopped_brownian}
        Naj bo $\{B_t\}_{t \geq 0}$ (standardno) Brownovo gibanje,
        $\tau$ čas ustavljanja glede na $\{\f_t\}_{t \geq 0}$
        in naj velja $P[\tau < \infty] = 1$.
        Potem je proces:
        \[
        \hat{B} := \{B_{T+t} - B_T \mid t \geq 0\}
        \]
        (standardno) Brownovo gibanje in je neodvisen od $\f_T$.
    \end{izrek}

    % Konec izreka

\bibliographystyle{plain}
\begin{thebibliography}{9}

\bibitem{karaktzas1991}
    Ioannis Karatzas and Steven E. Shreve. \emph{Brownian Motion and Stochastic Calculus}. Springer, 1991.

\end{thebibliography}
    
\end{document}